<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Add an Otter</title>
  </head>
  <body>
    <h1> Add an Otter</h1>
    <a href="/">Home</a>
    <% if(typeof(errors) != 'undefined' ) { %>
      <% for (var x in errors) { %>
       <h3><%= errors[x].message %></h3>
      <% } %>
    <% } %>
<hr/>
    <form action='/otters' method='post'>
    <table>
      <tbody>
        <tr>
          <td>Name:</td>
          <td><input type='text'  name='name' /></td>
        </tr>
        <tr>
          <td>Favorite Color:</td>
          <td><input type='text'  name='favorite_color' /></td>
        </tr>
        <tr>
          <td>Age:</td>
          <td><input type='text' name='age' /></td>
        </tr>
      </tbody>
    </table>
    <input type="submit" value="Create Otter" />
    </form>


  </body>
</html>
